services:
  nginx:
    build:
      context: .
    image: qrform:${IMAGE_TAG:-latest}
    container_name: react-nginx
    ports:
      - "5175:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge